<div class="tag-index">
  <% @tags.each do |tag| %>
     <%= link_to "#{tag.name}(#{tag.taggings_count})", tag_path(tag.name), class: "label label-default" %>
  <% end %>
</div>

<div class="map-index">
  <iframe src="https://www.google.com/maps/d/embed?mid=19AFTeknmIahUpgetKNOFbHfPjDuoeEoV" width="800" height="480"></iframe>
</div>
